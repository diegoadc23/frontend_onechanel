import { DadoVendaPeriodo } from './dado-venda-periodo.model';

describe('DadoVendaPeriodo', () => {
  it('should create an instance', () => {
    expect(new DadoVendaPeriodo()).toBeTruthy();
  });
});
