import { DadoVendaMarketplace } from './dado-venda-marketplace.model';

describe('DadoVendaMarketplace', () => {
  it('should create an instance', () => {
    expect(new DadoVendaMarketplace()).toBeTruthy();
  });
});
