import { ParametrosPesquisaProduto } from './parametros-pesquisa-produto.model';

describe('ParametrosPesquisaProduto', () => {
  it('should create an instance', () => {
    expect(new ParametrosPesquisaProduto()).toBeTruthy();
  });
});
